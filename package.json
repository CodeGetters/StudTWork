{
  "name": "studtwork",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": "true",
  "scripts": {
    "dev:backstage": "pnpm -F @studtwork/backstage dev",
    "dev:backend": "pnpm -F @studtwork/backend start",
    "dev:frontend": "pnpm -F @studtwork/frontend dev",
    "dev:lint": "pnpm -F @studtwork/frontstage lint & pnpm -F @studtwork/backend lint & pnpm -F studtwork/frontdesk lint",
    "dev:format": "pnpm -F @studtwork/backstage format& pnpm -F @studtwork/backend format",
    "build:backstage": "pnpm -F @studtwork/backstage build",
    "build:backstage-mock": "pnpm -F @studtwork/backstage build:mock",
    "preview": "pnpm -F @studtwork/backstage preview",
    "lint": "eslint . --ext .vue,.js,.ts,.jsx,.tsx --fix",
    "format": "prettier --write ./**/*.{vue,ts,tsx,js,jsx,css,less,scss,json,md}",
    "test:backstage": "pnpm -F @studtwork/backstage test",
    "test:backend": "pnpm -F @studtwork/backend test",
    "prepare": "husky install"
  },
  "lint-staged": {
    "*.{js,vue,ts,jsx,tsx}": [
      "prettier --write",
      "eslint --fix"
    ],
    "*.{html,css,less,scss,md}": [
      "prettier --write"
    ]
  },
  "engines": {
    "node": ">=16"
  },
  "keywords": [],
  "author": "CodeGetters",
  "license": "Apache 2.0",
  "devDependencies": {
    "@commitlint/cli": "^17.6.5",
    "@commitlint/config-conventional": "^17.6.5",
    "eslint": "^8.43.0",
    "eslint-config-prettier": "^8.8.0",
    "eslint-plugin-prettier": "^4.2.1",
    "husky": "^8.0.3",
    "lint-staged": "^13.2.2",
    "prettier": "^2.8.8"
  }
}
